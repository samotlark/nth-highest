
/**
 * Your task is to implement a method that returns nth highest
 * value present in the given array.
 * 
 * Examples:
 *  - findNthHighest([1, 2, 3], 1) === 3
 *  - findNthHighest([1, 5, 4], 2) === 4
 *  - findNthHighest([1, 5, 4], 3) === 1
 */
function findNthHighest(array, n) {
    return 0;
}

module.exports = findNthHighest;